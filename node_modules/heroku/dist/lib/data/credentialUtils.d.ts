import type { CredentialInfo } from './types.js';
export declare function sortByOwnerAndName(credentials: CredentialInfo[]): CredentialInfo[];
export declare function isOwnerCredential(cred: CredentialInfo): boolean;
