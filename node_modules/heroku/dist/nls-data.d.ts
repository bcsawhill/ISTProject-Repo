/**
 * Non-localized strings data.
 * Add new keys here as needed.
 */
export declare const nlsValues: {
    readonly 'pg:database:arg:description': "config var containing the connection string, unique name, ID, or alias of the database. To access another app's database, prepend the app name to the config var or alias with `APP_NAME::`";
    readonly 'pg:database:arg:description:default:suffix': ". If omitted, we use DATABASE_URL.";
    readonly 'pg:database:arg:description:arbitrary:suffix': ". If omitted, we use a random database attached to the app.";
    readonly 'pg:database:arg:description:all-dbs:suffix': ". If omitted, we use all databases.";
};
